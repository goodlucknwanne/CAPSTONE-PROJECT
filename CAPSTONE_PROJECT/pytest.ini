[pytest]
# Tell pytest where to look for tests
testpaths = tests

# Enable async test support automatically
asyncio_mode = auto

# Register custom markers if you want to group tests later
markers =
    asyncio: mark a test as an async coroutine